#include<stdio.h>
void input(char *name);
int has_nice_name(char *c);
void output(int res);

void input( char *name) {
  printf("enter the name");
  if(scanf("%c",name)!=1)
  {
    printf("Error");
    exit(0);
  }
}
int has_nice_name(char *c) {
  int c;

    
  }
}
